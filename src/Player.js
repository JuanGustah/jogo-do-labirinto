
export class Player {
  playerPosition;
  startPosition;
  finishPosition;

  constructor() { 
    this.startPosition = { x: 1, y: 1 };
    this.playerPosition = { x: 1, y: 1 };
    this.finishPosition = { x: 8, y: 1 };
    
  }

}